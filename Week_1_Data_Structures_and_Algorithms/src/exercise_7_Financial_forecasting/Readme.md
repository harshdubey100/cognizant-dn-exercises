Explain the concept of recursion and how it can simplify certain problems. Answer: recursion is just a process in which one function call itself again and again. until a specific condition reached. for example we have calculate the factorial of 4. there we need the fctorial of 3 and so n 2 and 1. so it will call very first factorial(4). in order to calculate factorial(4) we need factorial(3) beacuse the formula for factorial(4) = 4 * factorial(3). the factorial(4) will resides in stack until factorial(3) solved and the same process will happen til the base case 1. from factorial(1) it will return by solving. similarly here in this question we need the result of the previous year to calculate returnValue for the year.
Discuss the time complexity of your recursive algorithm. Answer: for this example we are iterating every time equal to the number of years. so linear time required.
Explain how to optimize the recursive solution to avoid excessive computation. Answer: recursion can be optimised by dynamic programming. sometimes we calculate the same funtion multiple times in the problem so rather than calculating for same value which was calculated before we store that ansd uses it if further required.